## 一、设计哲学

### 1.1 核心命题

> ***事实是判断的依据***
>
> 什么是事实？
> 
> **定义**：事实是相对确定的、静态的客观存在，但随时间可变、可更新。
> 
> 事实是一个相对确定的、静态的客观存在。如：个人身份信息：性别，出生年月，籍贯；完整的个人病历，企业的设备名称、规格，企业的操作规则，这是确定的事实。这些事实又是随时间可变的，必须随变化可随时更改。比如：病历新增的内容；企业更换设备；因业务变化修改规则。
>
> **精准的判断来自精确的目的和与目的相关的事实。**
>
> 所谓集中注意力，就是把与目的无关的事实做了排除，只集中于与目的相关的事实——**目的照亮事实**。
> 

当前大语言模型（LLM）已大规模应用于自动驾驶、医疗诊断、金融风控等关键领域，但行业绝不会直接裸用模型输出控制指令。根源在于：LLM 本身不携带设备专属目标、现场私有事实、历史风险判例——它只有概率。

TDA 架构不试图让 LLM "更聪明"，而是在 LLM 与执行器之间插入一个**本地轻量的校验层**，使 LLM 降级为"**策略建议器**"。所有输出必须通过双重受动极校验方可放行：***在源头既用事实约束纯模型发散与幻觉***。

> **双重受动极校验**：
> 1. **事实受动极** （ $R_{\text{fact}}$）：确认推理前提是否被当前事实快照支持；
> 2. **判例受动极** （ $R_{\text{case}}$）：确认该推理路径是否被历史风险记录否定。
>
> 任一受动极触发否定，系统即进入**受动极阻塞态**，能动极 $A$ 的生成意志被强制悬停。

### 1.2 受动极分层定律

受动极 $R$ 不是单一通道，而是**两个独立且不可合并的事实入口**：

| 子通道 | 读取对象 | 认识论功能 | 失效后果 |
|:---|:---|:---|:---|
| **$R_{\text{fact}}$** | 当前事实快照 $F(w_t)$ | **确认前提**：判断 $j=(P \Rightarrow Q)$ 中的 $P$ 是否被当前世界状态支持 | **误诊**：用旧知识替代当前事实 |
| **$R_{\text{case}}$** | 历史判例库 $C(w_{<t})$ | **负向约束**：即使 $P$ 被事实确认，该推理路径是否曾导致过不可接受后果 | **重蹈覆辙**：事实正确但策略错误 |

**关键区分**： $R_{\text{fact}}$ 回答「**现在是什么**」， $R_{\text{case}}$ 回答「**过去因此付出了什么代价**」。两者在认识论上不可归约——你不能用当前气压值推导出「上次在此气压下操作导致爆炸」，也不能用历史爆炸记录推导出「当前气压是多少」。

**为何不可归约为单一入口？**

既然都是"外部约束"，为何不能合并为一个统一的"知识库"？

因为"当前是什么"与"过去因此付出了什么代价"属于两种不可通约的认识论范畴。前者是**描述性**的，关乎世界状态的当下快照；后者是**规范性**的，关乎人类对历史后果的价值判断。一个气压计读数不能告诉你"上次在此气压下操作导致爆炸"，一个事故记录也不能告诉你"当前气压是多少"。若强行合并，要么让事实库背负价值判断而失去客观性，要么让判例库背负状态描述而失去历史纵深。分离不是工程便利，而是**认识论诚实**。

### 1.3 哲学的精髓与工程的底线

**刨根问底的"为什么"是无止境的。** 哲学的精髓就在于永不停止这个追问。

但工程的智慧在于**知道在哪里暂停追问**——不是为了欺骗自己到达了终点，而是为了**让行动成为可能**。

在这个意义上，这不是一个"数学真理"，而是一个"工程律令"（Engineering Imperative）：

> **如果你要建造一个承担后果的判断系统，你必须在架构中设定一个外部事实入口。这个入口不能由系统自身生成，否则追问将陷入自指循环。这不是因为外部入口提供了绝对真理，而是因为只有它才能把"错误的责任"外化到系统之外，使系统内部的判断获得可追溯性。**

**双重受动极的医学隐喻**：
-  $R_{\text{fact}}$ 是**化验单**：回答「患者当前血糖是多少」；
-  $R_{\text{case}}$ 是**禁忌症手册**：回答「该药物是否在类似病史中导致过过敏性休克」。

医生（ $A$）可以提出用药方案，但方案放行必须同时通过化验单确认和禁忌症排查。没有化验单，是**盲治**；没有禁忌症手册，是**冒险**。两者缺一不可，且不能互相替代。

### 1.4 三个基本假设

| 假设 | 含义 |
|------|------|
| **没有无目的的事实** | 事实只在特定使用者的特定目的下被提取和使用 |
| **没有无主人的事实** | 事实属于确认它的使用者，LLM 只代表使用者说话 |
| **没有无时间戳的事实** | 每个事实都是某个时间刻度上的静态快照 |

### 1.5 与主流 AI 安全方案的根本差异

| 维度 | 对齐/护栏方案 | 端到端+安全员 | TDA 架构 |
|------|-------------|-------------|---------|
| **约束来源** | 人类反馈（主观偏好） | 隐含在训练数据（不可追溯） | 使用者确认的客观事实（可追溯） |
| **可解释性** | 低（黑箱权重） | 中（人工事后记录） | 高（每次拦截可还原至具体事实/判例） |
| **干预时机** | 训练时（预设） | 输出后（补救） | 输出前（预防） |
| **错误责任** | 分散（训练数据、标注者、模型） | 归于人工审核者 | 外化至事实库与判例库的主人 |

#
### 1.6 判断归机、目的归人、事实为锚。

> 通过建立“事实强制读取（受动极）→ 能动极自由生成 → 辩论层冲突制衡 → 裁决AI动态决断 → 熔断器兜底中断 → 判例库闭环学习”的六步结构，使系统的每一次输出都可追溯至具体事实确认者和具体判例依据。最终决策主体不是一个“人”或一个“模型”，而是这个完整结构的累积权重——其合法性来自结构本身的制衡与可形式化、可编码、可审计、可问责，而非来自单点智能的完美性。这其中，人保留最高否决权（人工审计），模型保留判断执行权，事实保留最高优先级。

#
